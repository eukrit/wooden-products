# cloudbuild.yaml
# GO Corporation — GCP Cloud Build Pipeline
# Triggers: push to main (deploy) | push to dev/* (build+test only)

substitutions:
  _SERVICE: your-service-name        # Cloud Run service name
  _REGION: asia-southeast1           # Bangkok-nearest region
  _REPO: gcr.io/$PROJECT_ID          # Container registry
  _NOTION_DASHBOARD: https://www.notion.so/gocorp/Coding-Project-Dashboard-Claude-32c82cea8bb080f1bbd7f26770ae9e80

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_MEDIUM

steps:
  # Step 1: Install dependencies
  - name: 'node:20'
    id: install
    entrypoint: npm
    args: ['ci']

  # Step 2: Run tests
  - name: 'node:20'
    id: test
    entrypoint: npm
    args: ['test']
    env:
      - 'NODE_ENV=test'

  # Step 3: Run verify.sh (API + integration checks)
  - name: 'node:20'
    id: verify
    entrypoint: bash
    args: ['./verify.sh', '--ci-mode']
    env:
      - 'CI=true'

  # Step 4: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    id: build-image
    args:
      - 'build'
      - '-t'
      - '$_REPO/$_SERVICE:$SHORT_SHA'
      - '-t'
      - '$_REPO/$_SERVICE:latest'
      - '.'

  # Step 5: Push image
  - name: 'gcr.io/cloud-builders/docker'
    id: push-image
    args: ['push', '--all-tags', '$_REPO/$_SERVICE']

  # Step 6: Deploy to Cloud Run (main branch only)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: deploy
    entrypoint: bash
    args:
      - '-c'
      - |
        if [ "$BRANCH_NAME" = "main" ]; then
          gcloud run deploy $_SERVICE \
            --image $_REPO/$_SERVICE:$SHORT_SHA \
            --region $_REGION \
            --platform managed \
            --allow-unauthenticated
          echo "Deployed $_SERVICE:$SHORT_SHA to Cloud Run"
        else
          echo "Branch $BRANCH_NAME: skipping deploy (build+test only)"
        fi

  # Step 7: Post-build — update CHANGELOG and notify
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: post-build
    entrypoint: bash
    args:
      - '-c'
      - |
        echo "Build $SHORT_SHA completed at $(date -u)" >> CHANGELOG.md
        echo "Branch: $BRANCH_NAME | Trigger: $TRIGGER_NAME" >> CHANGELOG.md
        echo "Post-build record written."

images:
  - '$_REPO/$_SERVICE:$SHORT_SHA'
  - '$_REPO/$_SERVICE:latest'

tags:
  - '$_SERVICE'
  - '$BRANCH_NAME'
  - '$SHORT_SHA'

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/peak-api-token/versions/latest
      env: 'PEAK_API_TOKEN'
    - versionName: projects/$PROJECT_ID/secrets/n8n-webhook-key/versions/latest
      env: 'N8N_WEBHOOK_KEY'
